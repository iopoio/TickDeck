# seed 덱 실행 계획 — 2026-07-08 (클차장 설계·후추님 방향 7/6 밤 지시 기반)

> 목표 한 줄: **"당신 자료에 우리가 이만큼 더 얹었다"를 4주 안에 실물+반응으로 검증한다.**
> 이 문서가 이 트랙의 SoT. HANDOFF_2026-07-06 부록 4의 착수 순서를 상세화한 것.

## 0. 판단 (설계 결정 — Fable 5)

1. **새 필드를 만들지 않는다.** `00_intake.json.provided_sources[]`(레버1)가 seed 그 자체다.
   정식화 = 수령 관례 + provenance 표기 + 계약 게이트. 파이프라인 개조 아님.
2. **seed의 상품 가치 = 출처 부록의 구분 표기.** "제공 자료 n건 / 추가 조사 m건"이
   눈에 보이는 순간이 가치 증명이므로, 렌더+계약까지 가야 1단계 완료다.
3. **검증 면제 없음.** seed 자료도 티어·COI·수치 검증 동일 적용 — "당신 자료를 믿고
   그대로 옮김"이 아니라 "검증해서 얹음"이 서비스다.
4. **엔진 개선 동결.** 이 계획 밖의 렌더·문체 커밋은 파일럿 반응 확인 전 금지
   (7/7 하루 15커밋 패턴 차단). SaaS 층(Gemini 파이프라인)도 검증 종료까지 동결.

## 1. 작업 분해

### T1. 인테이크 정식화 — 담당: 클차장(문서층) ✅ 7/8 반영
- `intake-director.md`: seed 수령 관례 `_workspace/<run>/seed/` + 요청문·폴더 자동 인식 명시
- `collector.md`: seed 항목 `provenance:"seed"` 마킹 + **seed가 덮는 갈래는 확장 수집 제외**
  (중복 수집 금지) + C10 정본 카운트는 seed 포함 합산
- `verifier.md`·`deck-harness/SKILL.md`: source_registry에 provenance 전파 + seed 흐름 요약

### T2. 신뢰표면 코드 — 담당: Codex (스펙 = 이 문서 §3)
- `render_deck.py`: 출처 부록을 "제공하신 자료 / 추가 조사" 2구획으로 (provenance 기준)
- `contract_checks.py`: **C12 seed 무결 게이트** 신설
  - 발동 조건: `00_intake.json.provided_sources` 비어있지 않음
  - (a) provided_sources 전건이 source_registry에 존재하고 `provenance=="seed"`
  - (b) 렌더 HTML 부록에 두 구획 마커 존재
- `test_contracts.py`: C12 통과/실패 픽스처 각 1건 (기존 28건 불변 통과)

### T3. 파일럿 1런 — 담당: 후추님(자료) + 클차장(런 지휘)
- 후추님이 실제 PDF·링크 몇 개를 `_workspace/<run>/seed/`에 넣으면 e2e 1콜
- 산출물 = CLO 담당자 재접촉용 실물 (노-애스크 선물 방식·메시지 초안은 클차장)
- CLO 재료 재사용 가능. 새 주제여도 됨 — **자료가 실존하는 주제**가 유일 조건

### T4. 검증 채널 2개 병행 — 담당: 후추님(발송·선택) + 클차장(후보 조사)
- ① CLO 담당자: 노-애스크 전달 ("피드백 반영해 당신 자료 기반으로 다시 만들었다")
- ② 위시켓 RFP: 공고 첨부자료를 seed로 대응 덱 → 제안서 부착. 관계 비용 0인 본선 채널
  (freelance-opportunity-triage 스킬로 공고 판정)

## 2. 일정·게이트

| 주차 | 일 | 완료 기준 |
|---|---|---|
| 1주 (7/8~) | T1+T2 | `run_contracts.py` C1~C12 전체 PASS + seed 픽스처 렌더에 구분 부록 |
| 2주 | T3 파일럿 | seed e2e 1런 · 계약 위반 0 · 후추님 검수 통과 |
| 3주 | T4 발송 | CLO 전달 + RFP 1건 제안 제출 |
| 4주 | **go/kill 게이트** | 반응 신호 판정: 강함→클로즈드 베타 5명 모집 / 약함→멈추고 재판단 (kill 게이트 존중) |

**게이트 판정 기준(미리 박아둠)**: "강함" = CLO 담당자 실질 반응(질문·추가 요청) 또는 RFP 수주·미팅 1건.
정중한 감사 인사만 = 약함으로 센다.

## 3. Codex 위임 스펙 (T2)

- 대상 파일: `.claude/skills/deck-harness/scripts/render_deck.py`,
  `.claude/skills/harness-contracts/scripts/contract_checks.py`, `.../test_contracts.py`
- 수용 기준: `python3 .claude/skills/harness-contracts/scripts/test_contracts.py` 전건 PASS
  (기존 케이스 불변 + C12 신규 2케이스)
- 건드리지 말 것: C1~C11 기존 로직·렌더 본문 슬라이드 생성부·계약 출력 포맷
- provenance 규약: source_registry 항목 `provenance` 필드, 값 `"seed"|"research"`,
  **필드 부재 시 `"research"`로 간주** (하위호환 — 기존 run 재렌더 깨지지 않게)

## 4. 모델 라우팅 (이 트랙)

| 일 | 모델 |
|---|---|
| 이 계획·에이전트 문서 편집·Codex diff 검수·파일럿 QA 판정 | Fable 5 (세션 종료 후엔 오퍼스 4.8) |
| T2 코드 | Codex |
| 파일럿 반복 렌더·계약 재검 | Sonnet |

## 5. 미결

- ~~파일럿 주제·seed 자료~~ ✅ 7/8 완료 — `20260708_geo_seed` (GEO·제로클릭 덱, 전 게이트 통과)
- 위시켓 후보 공고 = 3주차 전까지 클차장이 2~3건 추려 보고 (**RFP는 1건 아닌 2~3건 제안** — 7/8 표본 확대 결정)
- ~~SaaS 층 동결 선언문~~ ✅ CLAUDE.md 반영됨

## 6. 게이트 재설계 (7/8 페이블 세션 — 후추님 합의)

- **판정 원칙: 돈이 방향을 정한다.** 서비스 신호(CLO·RFP)→수주 트랙, Phase 3·5 계속 동결 / 블로그 인바운드→B형에 산소 / 반응 0→널 분기.
- **널 분기 사전 확정: TickDeck = 펩핀치 콘텐츠 엔진으로 강등** (폐기 아님·유지비 ~0). Gemini 생성 코어 폐기 확정, 껍데기만 재사용. 신뢰 레이어는 덱 외 산출물로 이전 가능 자산.
- **반응 0의 해석**: "수요 없음" 단정 금지 — ①표본 ②가치 ③신뢰 ④가격 4가설 중 뭐가 죽었는지 구분 (CLO는 열람 확인 가능한 링크형 전달·블로그 인바운드 계측 병행).
- 상세 = 글로벌 메모리 `tickdeck-strategy-revenue-led` + 주간 런 관례 `PLAN_WEEKLY_RUN.md`.
