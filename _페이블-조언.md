# 페이블 조언 — TickDeck (2026-07-07)

## 현재 상태

"URL/자료 → 발표 덱" 자동 생성 제품. 엔진이 둘이다: ① SaaS 층(FastAPI+React+Celery+Gemini→python-pptx, Phase 1~5 완료)은 2026-04-17 이후 동결, ② v4 덱 하네스(`.claude/skills/deck-harness/`, Claude 에이전트 9단계→HTML/PDF, 계약 게이트 C1~C11)가 실제 품질 엔진이고 7/7 현재도 매일 커밋 중. 1콜 러너(`run_deck.sh`) 실측 = 덱 1개 $16/80분. 7/6 실무자 피드백("퍼플렉시티와 다르지 않다·내가 직접 조사하겠다")로 **자료 주입형(seed) 덱**으로 방향이 정해진 시점 — 엔진은 살아있고, 제품 형태가 재정의 중이다.

## 정리해야 할 것

3.1G 원인 실측 (전부 gitignore 상태·미커밋 변경 0):

| 항목 | 크기 | 판단 |
|---|---|---|
| `corpus/big3/` | 1.2G | Big3 PDF 코퍼스 208건. 자산은 맞으나 리포 안에 둘 이유 없음 — 리포 밖 공용 저장소로 이동 권장 |
| `v3/axis2_layouts/inbox/` | 651M | 레퍼런스 PDF 수집함. 패턴 추출(`PATTERN_LIBRARY.md`) 끝난 원본은 외부 이동/삭제 가능 |
| `.claude/worktrees/` | 460M | **stale worktree 3개** (cranky-elion·nice-maxwell·nifty-lederberg, 전부 detached e58fe03). `git worktree remove`로 즉시 회수 가능 |
| `_workspace/` | 266M | run 38개 누적. 최근 CLO v5/v5.1·비교 자산만 남기고 6월 이전 run 아카이브 |
| `v2/` | 239M | 구세대 엔진. `research_base/` 143M + 샘플 HTML 4개(M13~M16, 각 22~23M). v2는 참조 종료 판단되면 통째 아카이브 후보 |
| `.git` | 123M | pack 115MB — 과거 대용량 HTML이 히스토리에 있음. 당장 급하지 않음 |

기타 확인된 것:
- **브랜치 정리**: `main`이 6/21에 멈춰 있고 현재 작업은 전부 `deck/2026-mktg-v2-engine`에 있음(2주치 미머지). `claude/*` 브랜치 4개는 main과 동일 커밋(6/21) — 삭제 가능. `~/.gemini/antigravity/worktrees/TickDeck/restore-vscode-layout-view` worktree도 방치 상태.
- 루트의 `]+.pdf` (0바이트) — 오타로 생긴 쓰레기 파일, 삭제.
- `NEXT_STEPS.md` — 상단 한 줄만 7/6이고 본문 전체가 4/17 상태(제대리 Critical 수정 시점). `HANDOFF_2026-07-06.md`와 이중 SoT 상태 — 본문 갱신하거나 핸드오프 포인터로 축소.
- `제대리_Critical수정_지시서.md` — 4/17 완료된 작업의 지시서. 아카이브.
- `README.md` — SaaS 층만 설명하고 v4 하네스 언급 0. 현재 실체와 불일치.
- `.claude/skills/deck-harness/pptx_venv/` 13M — venv가 스킬 폴더 안에 있음. 동작하면 두되, 리포 복제 시 깨지는 요소임은 인지.

## 더 발전하려면

1. **main 머지부터** — 2주치 핵심 작업(계약 게이트·1콜 러너·시장조사 장르)이 전부 작업 브랜치에만 있다. 백업·이어보기 관점에서 가장 싼 보험. 머지 후 claude/* 브랜치 4개와 stale worktree 3개 정리(즉시 460M 회수).
2. **SaaS 층 거취 결정** — backend/frontend/shared는 4~5월 이후 동결이고, seed 덱 피벗이면 Gemini 3단계 파이프라인은 사실상 죽은 코드다. "Phase 3에서 v4를 감싼다"는 계획이 유효한지 재판정하고, 아니면 v2처럼 명시적 동결 선언. 두 엔진 괴리는 CLAUDE.md에 경고만 있고 결정이 없다.
3. **원가 라우팅 (로드맵 Phase 4 앞당기기)** — $16/덱은 designer 개정 루프(~390k 토큰)가 지배적. 단계별 모델 지정 표가 CLAUDE.md에 이미 있으니 run_deck.sh에 기계적으로 강제하고 `_cost_log.jsonl`로 회귀 감시.
4. **QA 자동화 갭 메우기** — 핸드오프의 정직한 자기평가대로 "1~4차 품질의 동력은 후추님 피드백 5회"였다. 7/6에 규칙화한 풀어쓰기 7규칙·READER_FIRST_JARGON을 qa_lint 기계 게이트로 승격하는 작업이 무인 서비스의 전제.
5. **대용량 자산 외부화** — corpus·v3/inbox·research_base 합계 ~2G를 리포 밖(`~/Data/tickdeck-corpus` 등)으로 옮기고 경로만 참조. 리포는 코드+계약+스킬만 남기면 300M 이하.

## 앞으로 나아가야 할 방향 (심층)

**핵심 진단**: 7/6 실무자 검증은 실패가 아니라 이 프로젝트에서 가장 값진 데이터다. "공개주제를 AI가 조사해 덱으로"는 퍼플렉시티·감마와 정면 승부라 이길 수 없고, 실무자에게 완성 덱은 "내 일을 대체하는 물건"이라 채택이 안 된다. seed 덱("당신 자료에 우리가 이만큼 더 얹었다")은 이 둘을 동시에 푸는 유일하게 확인된 가설이다 — 실무자의 "내가 조사했다"가 유지되고, 검색이 못 하는 개인 맥락 확장이 차별점이 된다.

**단기 (1~2개월) — 가설 검증에 전부 걸기**
- 부록 4 착수 순서 그대로: `seed_materials[]` 인테이크 정식화 → seed 최우선 소화 + C10 확장 수집 → 출처 부록에 "제공 자료 vs 추가 조사" 구분 노출. 파이프라인 개조가 아니라 입구 정식화라는 판단이 맞다 — 2~3주 일.
- 파일럿 1런을 **CLO 담당자 재접촉용 실물**로 만들 것. "당신이 모은 고급 PDF들 + 우리가 얹은 것"을 그 담당자에게 다시 보여주는 게 Phase 0 수요 게이트의 진짜 재실행이다. 이 반응이 약하면 4주 kill 게이트대로 멈추는 것까지 포함해서.
- 엔진 개선은 seed 덱에 필요한 것만. 지금처럼 렌더·문체 룰을 매일 다듬는 건 수요 신호 확인 전엔 과투자다(7/7 하루 커밋 15건 — 에너지 배분 주의).

**중기 (6개월) — 신호가 살아있을 때만**
- 제품 형태: "덱 생성기"가 아니라 **"자료 검증·확장 조사 서비스(출력이 덱)"**로 포지셔닝. Gamma/Copilot은 예쁜 슬라이드 레드오션이지만, Tier-A PDF 게이트(C10)·출처 계약·검증 보드는 그쪽에 없는 자산이다. 신뢰표면이 곧 상품이다.
- 수익화: $16/덱 원가면 B2C 불가, B2B 단건 고가(시장조사·제안 대응 덱, 건당 수만~수십만 원)가 유일한 마진 구조. 원가 라우팅으로 $5 이하 만들기 전엔 구독 모델 금지. rfp_planner가 이미 있는 걸 보면 RFP 대응 덱이 자연스러운 첫 유료 니치.
- 서비스 래핑은 기존 FastAPI/Celery/OAuth 재활용(로드맵 Phase 3·5) — 새로 만들 것 거의 없음. 단 클로즈드 베타 5~10명 확보 후에만.

**지속가능성 리스크 2개 (직시할 것)**
1. **플랫폼 종속**: 엔진이 `claude` 헤드리스 1콜 = Claude Code CLI에 묶여 있다. 정액 구독이라 지금 원가가 싸 보이는 것도 착시일 수 있다(유료 서비스로 팔면 종량 전환 필요). 계약 게이트를 모델 무관으로 유지하는 현재 방침이 맞고, Phase 4에서 최소 1개 단계는 타 모델로 실제 치환 검증해 둘 것.
2. **1인 회복기 페이스**: 이 리포의 강점은 핸드오프·계약·회고가 시스템화돼 있다는 것. 약점은 방향 전환이 잦아(v2→v3→v4 SaaS→하네스→seed) 동결 층이 쌓인다는 것. 층을 하나 추가할 때마다 하나를 명시적으로 죽이는 규율이 3.1G와 두-엔진 괴리의 근본 처방이다.
