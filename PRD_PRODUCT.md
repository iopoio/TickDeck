# TickDeck 제품 PRD — "딸깍" (입력 → 버튼 → 덱)

> 2026-07-25 초안. 후추님 방향 결정: **질은 상수(안 내림), 속도는 점진적으로 단축.**
> 관계: `PRD_v4.md` = **엔진 명세**(좋은 덱을 어떻게 만드나) · **본 문서 = 제품 명세**(사용자가 딸깍하면 원가·시간 안에서 덱이 나오나) · `PRODUCT_ROADMAP.md` = 실행 일정(본 PRD의 단계 실행).
> status: 초안 · **wedge(누구·어떤 덱) 결정 대기** — 그게 채워져야 §2 속도·§3 질 목표선이 확정됨.

---

## 0. 딸깍 정의 (한 줄)

사용자가 **주제/자료를 넣고 버튼 한 번** 누르면, **v4 품질**의 프레젠테이션 덱이 **원가 안에서** 나와 다운로드된다.

- **사용자 노동 = 버튼 한 번** (체감 딸깍). 실제 생성은 백그라운드.
- **딸깍 ≠ 즉시.** "만드는 중 → 완료 알림"(비동기)이 출발점. 즉시성은 속도 로드맵(§2)으로 점차 접근.
- 왜 v4 위에: 껍데기(웹·OAuth·잡큐·결제·다운로드)는 이미 있음(webtoslide 유산·§5). 없는 건 **엔진이 무인·저원가로 도는 것**.
- **첫 wedge**: 회사 전략기획자의 시장조사 덱(§4) — 범용 확장은 나중.

## 1. 두 축 원칙 (후추님 7/25 결정)

| 축 | 원칙 | 근거 |
|---|---|---|
| **질** | **상수 — 안 내린다.** v4 하네스(원본 분석·계약 C1~C7·검수) 그대로 유지. | 질이 TickDeck의 존재 이유. 내리면 Gamma·Claude Design 대비 차별 소멸 |
| **속도** | **점진 개선 축.** 비동기로 출발(느려도 됨) → 병렬화·검수루프 제거·라우팅·캐싱으로 단계적 단축. | 속도는 엔지니어링 문제라 시간이 풀어줌. 회복기 1인 페이스에 맞음 |
| **채택** | **질과 별개 축.** 질만 완벽해도 안 팔릴 수 있다(7/6 "직접 하겠다"). wedge에서 따로 검증(§3). | 질↑ ≠ 채택↑. 안전장치 |

## 2. 속도 로드맵 (질 고정 · 벽시계를 단계로 줄인다)

현재 실측: 덱 1개 **~810k 토큰 · 벽시계 2.5h** (`_workspace/20260705_lever1_e2e`, ROADMAP §Phase1). 단, designer 개정 4회 중 상당수는 **엔진 결함 수정 루프(1회성)** — 정상 run은 이미 더 쌈.

| 단계 | 속도 목표 | 수단 | 질 |
|---|---|---|---|
| S0 · 비동기 출발 | 무제한(버튼→알림) | 잡큐 래핑(기존 Celery) | v4 그대로 |
| S1 · 검수루프 제거 | ~십몇 분 | 1회성 엔진 결함 fix → 정상 run 안정화 | v4 그대로 |
| S2 · 병렬화 | ~몇 분 | 수집·분석 팬아웃 동시 실행·순차 구간 최소화 | v4 그대로 |
| S3 · 라우팅·캐싱 | 분 단위 | 단계별 모델 최적(분석=opus 유지·나머지 경량)·주제/장르 캐싱·프롬프트 캐싱 | v4 그대로 |

> 핵심: **질을 고정한 채 벽시계만 내린다.** 각 단계는 측정으로 검증(`run_deck.sh` → `_workspace/_cost_log.jsonl`).
>
> **7/27 실측 — 하이브리드 designer 채택**: designer를 자유 HTML 병렬 생성(v4.5·designer.md 하이브리드 모드)로 교체 → 40분→11분·시각QA 1발·사후 C6 감사 성립. S1의 최대 병목이 해소됨. pptx 네이티브 주문만 spec 경로 병행.
>
> **wedge 감내선**: 전략기획자 시장조사는 데드라인 기반이라 **S1(십몇 분·비동기)이면 이미 딸깍 성립** — "며칠 걸리던 0→초안"을 십몇 분으로. S2·S3(몇 분·분 단위)는 즉시성보다 **원가 절감·동시 처리량(규모 확장)**용. **완벽한 속도를 기다리지 않고 S1에서 출시 가능** — 후추님 "속도 점진"의 실전 의미.

## 3. 채택 게이트 (질 ≠ 팔림 · 7/6 교훈)

- **문제**: 7/6 Phase C에서 v4 풀 품질 덱을 실무자가 "직접 조사해서 내겠다" — 극대화된 질이 **채택**으로 안 이어짐.
- **원인 가설**: 질(원본 분석 깊이)과 채택(바로 쓸 수 있음·실무 적합)이 다른 축. 완벽한 분석이 "내 발표에 그대로 쓸 초안"과 다름.
- **게이트**: 질은 안 내리되, **"이 wedge 사용자가 결과 덱을 실제로 채택/결제하는가"**를 Phase 0(쇼케이스)·클로즈드 베타에서 신호로 확인. 신호 약하면 **딸깍 진행 보류**(만들 이유 게이트).

## 4. wedge — 회사 전략기획자의 시장조사 덱 (후추님 7/25 결정)

풀 SaaS(범용) 말고 **가장 좁은 첫 사용자 하나**부터.

- **누구**: 회사 전략기획·신사업 담당자 (기획팀·전략팀). 후추님 = customer zero(15년 기획자)
- **어떤 덱**: 시장 상황 조사 — 시장 규모·트렌드·경쟁·기회. **v4 `genre-market-research`·`genre-trend-report`와 정확히 정합** (엔진이 이미 잘하는 것)
- **용도**: 내부 보고·의사결정 자료 → **임원 보고급 출처·논리 요구**(질 못 낮춤 = §1 질 상수와 일치)
- **왜 딸깍**: 반복적 시장조사를 0(백지)에서 시작하는 시간이 아픔. 근데 임원 보고라 질은 타협 불가
- **채택 각도 = 완제품 대체 X · "0→고품질 초안" 시간 단축.** 기획자가 딸깍으로 초안 받고 자기 회사 맥락으로 다듬음. (7/6 "직접 하겠다"의 정면 대응: 전문가를 대체하려 말고 그의 0→1을 없앤다)
- **가격 감**: B2B(회사 비용)·덱당 or 구독. 원가(덱당 몇 $) < 가격 여유 큼

### 4.1 이 wedge의 리스크 (정직 — Phase 0에서 검증)
1. **전략기획자 = 조사 전문가**라 "내가 하는 게 낫다"(7/6 재현)가 가장 잘 나올 사용자. → 채택 각도(완제품 아닌 초안 단축)로 정면 대응.
2. **회사·산업 특수 맥락**을 범용 딸깍이 못 담으면 "일반론"으로 버려짐. → 입력에 사용자 자료·맥락 주입(seed 덱 방향·NEXT_STEPS 7/6)으로 특수성 흡수.
3. **founder-market fit이 강점이자 함정** — 후추님 통증이 시장 통증인지 Phase 0에서 *관찰*(짐작 금지·gap self-check).

## 5. 원가 모델

- 철칙: **덱당 원가 < 가격.** 안 되면 사업 성립 X.
- 원가 = 토큰(엔진) + 인프라. 질 고정이라 토큰은 §2 라우팅·캐싱으로만 절감.
- 측정 우선(ROADMAP Phase 1) → 마진 확보(Phase 4) 후 가격 확정. **B2B(회사 비용)라 개인 대비 지불의사 높음** — 덱당 or 월 구독. 원가 여유 큼.
- **도메인 registry 재사용 = 반복 주문 마진 구조 (7/27)**: 첫 주문은 수집+검증 풀코스(실측 ~527k tok·50분)지만, 같은 도메인 재주문(분기 업데이트·후속판)은 기존 `02_verified` registry 증분 갱신만 — **2회차부터 원가 절반 이하**. 가격은 유지되므로 반복 고객(구독형)에서 마진이 구조적으로 상승. 원가 실측(트랙 B)에 "재주문 시나리오 1회" 포함할 것.

## 6. 기존 자산 (이미 있음 — 재사용)

- **제품 껍데기**: FastAPI·React·Celery 잡큐·Google OAuth·토큰 결제/환불·다운로드 = Phase 1~4 완료(webtoslide 유산·`NEXT_STEPS.md`). ⚠ 단 옛 "URL→슬라이드"용이라 **v4 입출력 스키마와 접합 검증 필요**(낙관 금지).
- **엔진**: v4 하네스(`.claude/skills/deck-harness/`) = 품질 엔진 ✓
- **없는 것**: 엔진 1콜화(수동→스크립트)·비동기 접합·원가 제어·무인 QA

## 7. 성공 / kill 기준

- 성공: wedge 사용자가 딸깍→채택→결제. 덱당 원가 < 가격. 벽시계 = 비동기로 감내 가능.
- kill: Phase 0 수요 신호 약함 / 원가 > 가격 구조적 / 속도가 비동기로도 감내 불가 / 회복기 페이스 초과.

## 8. 다음 (이 PRD를 살리려면)

1. **§4 wedge 확정** (후추님) — 나머지 목표선이 여기서 나옴
2. S0 비동기 래핑 + **첫 1콜 run 깨끗한 원가·벽시계 실측**
3. Phase 0 쇼케이스로 채택 신호 확인 → go/보류
