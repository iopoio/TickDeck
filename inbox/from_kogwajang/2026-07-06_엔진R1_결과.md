# 2026-07-06 엔진 R1 결과

## 무엇 만들었나

- `source-row`를 한 줄 hidden clip에서 2줄 wrap + `+N` 축약 방식으로 변경.
- `capture_deck.sh` FIT 게이트에 `source-row/source-link` hidden·nowrap clip 검출(`FIT_SOURCE_CLIP`) 추가.
- viz series role 검증 추가: `baseline`/`highlight` 기존 유지 + `negative`/`positive`/`brand` 추가. 기존 `benchmark`/`left`/`right`도 명시 허용.
- `quarterly_bars` chart 추가: on-bar label, hidden axis 옵션, axis break `≈`, 마지막/비교 분기 accent.
- `fin_table` chart 추가: current column outline, group/sub/ratio row, negative red/paren, text cell 허용.
- 데모 spec/HTML: `_workspace/20260706_engine_r1_demo/`

## 작업별 결과

### 작업 0: source-row overflow + 게이트

- 구현: 통과.
- clo_v51 재렌더: `python3 ... render_deck.py _workspace/20260706_clo_v51/06_deck_spec.json ... -o deck.html` → 19 pages HTML 생성.
- p08 HTML 확인: `source-row`가 `한경비즈니스/핸드메이커/비즈체크/서울경제 +6`으로 축약됨.
- 게이트 보강 테스트: `test_capture_deck_detects_hidden_source_row_clips` 통과.
- [미검증] 실제 PDF 캡처 전/후 비교: Chrome headless가 `Abort trap: 6`으로 실패. Playwright Chromium도 macOS Mach port permission denied. 새 PDF는 생성 못 함.

### 작업 1: CL-semantic_color

- 구현: 통과.
- `negative` → `var(--semantic-negative)`, `positive` → `var(--semantic-positive)`, `brand` → series color 또는 `var(--semantic-brand)`.
- contract role 검증 추가. unknown role `decorative` reject 테스트 통과.
- 데모 HTML marker 확인: negative/positive token, brand `#F7931A` 렌더 확인.

### 작업 2: CH-quarterly_bars

- 구현: 통과.
- 신규 chart type `quarterly_bars` 추가.
- on-bar label class `quarter-value-onbar`, axis break `≈`, highlight 분기 accent 확인.
- contract + renderer 1:1 테스트 통과.

### 작업 3: CH-fin_table

- 구현: 통과.
- 신규 chart type `fin_table` 추가.
- `columns`, `accent_column`, `negative_style`, row `cells` 스키마 추가.
- text cell은 숫자 없는 텍스트만 허용. raw number text cell reject 테스트 통과.
- 데모 HTML marker 확인: `fin-accent-column`, `fin-row-group`, `fin-row-sub`, italic ratio row, `(12 bn)`, `var(--semantic-negative)`.

## 검증 수치

- RED 확인: 신규 테스트 6개는 구현 전 fail/error 확인.
- GREEN 확인: 신규/관련 테스트 8개 통과.
- 전체 계약 테스트: `python3 .claude/skills/harness-contracts/scripts/test_contracts.py` → 84/84 pass, 0 fail, 0 error.
- 데모 render: 4/4 HTML 생성.
- qa_lint 데모: 4/4 exit 0. 단일 페이지 fixture라 `LAYOUT_MONOTONY` low 1건씩은 남음.
- key grep: 변경 범위 actual key pattern 0건. repo-wide grep은 기존 `v2/sample_M15_student_assignment.html` 압축 base64에서 false positive 1건.
- Gemini 교차 리뷰: [미검증] 2분 무응답으로 Ctrl-C 중단.

## Think 쪽 필요 변경

- TickDeck 내부 `capture_deck.sh`는 수정함.
- [미검증] Think/tools/deck_harness 쪽 별도 overflow 게이트 존재 여부는 이 세션에서 읽지 않음. 같은 copy가 있으면 `sourceClips`/`FIT_SOURCE_CLIP` 로직 이식 필요.

## 미구현·미검증

- 실제 PDF 시각 캡처: 미검증. 원인: Chrome/Playwright/QuickLook이 현재 sandbox에서 실행 실패.
- 브랜치/커밋: 미수행. 원인: `.git/refs` write denied.
  - `git switch -c deck-harness/layout-variety` → `unable to create directory for .git/refs/heads/deck-harness/layout-variety`
  - `git switch -c deck-harness-layout-variety` → `.git/refs/heads/deck-harness-layout-variety.lock: Operation not permitted`

## commit 해시 목록

- 없음. 위 `.git/refs` write denied로 commit 생성 불가.

## 다음 라운드 R2 제언

- 브라우저 캡처 권한 문제 먼저 해결 후 `capture_deck.sh` actual before/after를 재실행.
- TY-source_infra는 R2에서 source를 page-level이 아니라 viz/series 단위로 스키마 설계 후 착수.
- semantic color는 R1에서 막대/라인/표 계열 중심 적용. R2에서 gauge/donut/pictogram 등 단일 KPI 차트까지 role 색 적용 범위를 넓힐지 결정.
