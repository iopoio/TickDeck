## 클과장 회고 — 2026-04-16

**무엇을 틀렸나:** .gitignore에 `.env.*` 패턴 작성 시 `.env.example`도 함께 제외됨. 첫 커밋에서 .env.example 누락.

**왜 틀렸나:** `.env.*`가 `.env.example`을 포함한다는 걸 작성 시점에 검토하지 않았음. 제대리 초안을 그대로 가져오면서 패턴 충돌 검증을 건너뜀.

**다음엔:** .gitignore 작성 후 `git status`로 예상 추적 파일 목록 확인 → 누락/과잉 제외 없는지 커밋 전에 반드시 점검.
