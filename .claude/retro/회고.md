# TickDeck 회고 (누적)
> 클과장 + 제대리 합본. 동일 실수 반복 감시용 카운터 포함.

---

## 🔢 실수 카운터

### 클과장
| # | 패턴 | 발생 | 마지막 |
|---|---|---|---|
| C1 | .gitignore 패턴 충돌 검증 누락 (.env.* vs .env.example) | 1회 | 2026-04-16 |
| C2 | 2파일 이상 디버깅을 제대리에 넘기지 않고 직접 처리 (역할 분리 위반) | 1회 | 2026-04-16 |
| C3 | 리포 네이밍 혼동 (기존 TickDeck 실제 내용은 WebToSlide) | 1회 | 2026-04-17 |

### 제대리
| # | 패턴 | 발생 | 마지막 |
|---|---|---|---|
| (기록 없음 — Antigravity Critical 3건 자율작업 깔끔하게 완료) | | | |

### 공통
| # | 패턴 | 클과장 | 제대리 | 총 |
|---|---|---|---|---|
| (없음) | | | | |

---

## 2026-04-16 15:30 — .gitignore 패턴 충돌

### 클과장
**무엇을 틀렸나**: .gitignore `.env.*` 패턴이 `.env.example`까지 제외시켜 첫 커밋에 누락

**왜 틀렸나**: 제대리 초안을 그대로 가져오면서 패턴 충돌 검증을 건너뜀

**다음엔**: .gitignore 작성 후 `git status`로 예상 추적 파일 목록 확인 → 누락/과잉 제외 없는지 커밋 전에 반드시 점검

---

## 2026-04-16 15:39 — 역할 분리 위반

### 클과장
**무엇을 틀렸나**: 복잡한 디버깅(uvicorn 실행 경로, 다중 파일 수정)을 제대리에게 넘기지 않고 혼자 처리함

**왜 틀렸나**: "니가 할 수 있을껄?" 물음에 기술적 가능성만 봄. 역할 분리 규칙(Think CLAUDE.md) 확인 안 함

**다음엔**: 코드 수정이 2개 파일 이상이거나 디버깅 사이클 2회 이상 예상되면 제대리에게 먼저 넘김. 클과장은 설계/판단/리뷰만

---

## 2026-04-17 — 코드 리뷰 (미수정, 참고용)

### 제대리 총평
"Automation 폴더에서 아키텍처적으로 가장 견고한 프로젝트. 비동기 아키텍처 + 분리된 워커 프로세스 + Pydantic/TypeScript 타입 안전성이 프로 수준."

### Critical 3건 (미착수)
1. **Google OAuth 완전 비활성화** — `App.tsx:14` DEV_TOKEN 하드코딩. 프로덕션 배포 불가
2. **토큰 잔액 API 항상 0** — `tokens.py` 하드코딩
3. **Celery 트랜잭션 불일치** — psycopg2 동기 호출로 asyncpg 우회. 워커 크래시 시 토큰 손실 가능

### High 4건
4. Surrogate 문자 처리 3곳 중복
5. PPTX 임시 파일 삭제 정책 없음
6. EditorPage 중간 저장 없음
7. 크롤러 12,000자 + Gemini 8,000자 이중 제한

### 수정 Top 3 (예상 8~12일)
1. Google OAuth 구현 (2~3시간)
2. Celery 트랜잭션 + 에러 처리 (3~4시간)
3. 토큰 잔액 + PPTX 파일 정리 (2~3시간)

### 현재 완성도 약 70%
Phase 1~4 완료, Phase 5(프로덕션 배포) 전 OAuth/토큰/Celery 수정 필요

---

## 2026-04-17 오후 — Critical 3건 완료 + 리포 정리

### 제대리 (Antigravity 자율 작업)
**작업 범위**: Critical 3건 모두 구현

1. **Google OAuth 완성**
   - `backend/routers/auth.py` — 콜백에서 `RedirectResponse`로 `{frontend_url}/auth/callback?access_token=...` 리다이렉트
   - `frontend/src/lib/auth.ts` 신규 — JWT localStorage 유틸
   - `frontend/src/pages/LoginPage.tsx` / `CallbackPage.tsx` 신규
   - `frontend/src/App.tsx` DEV_TOKEN 제거

2. **토큰 잔액 실제 조회**
   - `backend/routers/tokens.py` — 실제 `TokenBalance` DB 쿼리

3. **Celery 트랜잭션 + 환불 로직**
   - `backend/models/generation.py` — `lock_tx_id` ForeignKey 추가
   - `backend/alembic/versions/a1b2c3d4e5f6_add_lock_tx_id_to_generations.py` 마이그레이션 신규
   - `worker/tasks/generate.py` — `_refund_token()` + `autoretry_for=(TransientError,), max_retries=3, countdown=5`

**결과**: 거짓 보고 없음. 계획 승인 후 완료까지 깔끔하게 진행.

### 클과장
**작업**: 보안 이중 검증(4-pass) + 커밋/푸시 + GitHub 리포 분리

**리포 네이밍 혼선 해결**:
- 기존 `iopoio/TickDeck` 리포의 실제 콘텐츠는 WebToSlide 프로토타입 → `iopoio/webtoslide`로 rename
- 진짜 TickDeck 프로젝트용으로 `iopoio/TickDeck` 신규 생성 후 깨끗한 히스토리로 push

**보안 조치**:
- `.env`의 `GOOGLE_CLIENT_SECRET` 노출 의심 → Google Cloud Console에서 재발급
- `.env` 갱신 완료 (`GOCSPX-...Jp`)
- 커밋 히스토리 4-pass 검사 — 누수 없음

**C3**: 리포 네이밍 혼동 추가 — 다음엔 리포 생성 시 내용과 이름 일치 여부 검증
