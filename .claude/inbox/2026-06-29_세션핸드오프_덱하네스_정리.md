# 세션 핸드오프 — 덱/하네스 정리 (2026-06-29 새벽)

> 다음 세션: **이 파일 먼저 읽고, ④ 미해결부터 후추님과 정리. 빌드는 그 다음.** 지난 세션에서 클차장이 v3/v4·세 하네스를 머릿속에서 뭉개 헷갈림 → 실제 파일 기준으로 다시 적음. 후추님 밤샘·기력 소진 상태로 종료. 작업물은 *하나도 안 날아갔음*(아래 ⑤).

---

## ① 한 줄 상태
- **보고서 내용(SoT)** = `_workspace/20260628_marketing2026_v2/REPORT_2026_marketing_trends_clear.md` — **완성·문제 없음.** 사고는 전부 *덱 렌더/하네스 트랙*에서 났지 내용이 아님.
- 막힌 곳 = "어느 렌더러/하네스로 그 보고서를 *좋은 덱*으로 뽑느냐"가 **트랙이 갈려 혼선**.

## ② 세 개의 '하네스' — 실재 확인됨, 혼동 금지
| | 위치 | 정체 | 건드림? |
|---|---|---|---|
| **A. 공용 엔진** | `Think/tools/deck_harness/` | 범용·여러 프로젝트(publace·pepstock 등) 공용 렌더 엔진 | **절대 X** ([[project_deck_platform_tickdeck]]) |
| **B. 틱덱 파이썬 엔진** | `TickDeck/tickdeck_harness/` | `engine.py`(내용→HTML)+`marketing_full.py` 등. 6/27~28 GLM/Qwen/Kimi 흡수로 빌드됨([[project_deck_content_engine_2026_06]]) | ⚠️ **메모리 충돌**(아래 ④) |
| **C. v4 에이전트 하네스** | `TickDeck/.claude/skills/deck-harness/` | agents(editorial/page/designer…)+references(writing-standard·palettes·visualization)+`render_deck.py`+`capture_deck.sh`. PRD_v4. **이번 세션이 작업한 곳** | O (편집함) |

## ③ 덱 파일 (`_workspace/20260628_marketing2026_v2/`)
- `deck_v3.html` — **ad-hoc**(`render_distilled_deck.py`·`render_marketing_deck.py`로 만듦). 밤새 다듬음: 추림·이미지화(dumbbell·before/after)·출처위치·p9·T3 **수정 완료** / 표지·p10·p11·목차·paid-earned·3→5 다리는 **미반영(중단)**. *보기엔 제일 낫지만 시스템이 만든 게 아님(일회용 렌더러).*
- `deck_v4.html` — **C(v4 하네스)**가 만듦(`render_deck.py` ← `06_deck_spec_v2.json`). 구조 OK(5트렌드 묶기·S/A 분리) **but 렌더 깨짐**: 페이지마다 검은 띠·표지에 "표지"·stat_grid 잘림·비주얼 0·breeze 청록(펜톤 X).
- `deck_v2.html` — 더 옛 버전.

## ④ ⚠️ 미해결 — 후추님 결정 필요 (다음 세션 첫 할 일·빌드 전)
1. **트랙 단일화:** going-forward 덱 시스템은 **B(파이썬 engine.py)냐 C(v4 에이전트+render_deck.py)냐?** 둘 다 살아있고 둘 다 마케팅 덱을 만든 적 있음. 하나로 정해야 함.
2. **메모리 충돌 해소:** `project_tickdeck_harness_v4` = "파이썬 tickdeck_harness 실패·은퇴" / `project_deck_content_engine_2026_06` = "tickdeck_harness 파이프라인 빌드 완료·활성." **서로 모순.** 후추님께 어느 게 현재 진실인지 확인.
3. **v3 vs v4 산출물:** 2026 마케팅 덱의 결과물을 ad-hoc deck_v3로 마무리할지, 시스템(B 또는 C)으로 재생산할지.

> 지난 세션 클차장 실수: "v3가 좋으니 render_distilled에서 C로 *포팅*하자"고 코덱스 띄움 → 후추님 "v3는 잘못돼서 v4로 온 거 아니냐"로 중단. 트랙도 안 정하고 포팅부터 = 사고. **④ 정하기 전 렌더러 빌드 금지.**

## ⑤ 이번 세션에 한 것 (안 날아감)
- **deck-craft 학습**: 신야로 minimax/qwen/glm에 "우리 보고서로 덱 만들고 과정 설명" → `_workspace/_research/ai_deck_builds/` + `ai_deck_builds_back/`(9~18장). kimi는 빈출력 실패. **결론: minimax 방식 선호·qwen 이미지화 참신.**
- **추론 논리를 C 에이전트에 박음**(규칙 아니라 질문+WHY·일반화):
  - `editorial-director` = 뼈대 한 문장·T-S-E-A 라벨·**★3~5개로 묶기(MECE)**·적 정의·결론 끝.
  - `page-planner` = "회사 가서 한 문장" 질문·가위/확대경·**★S와 A 분리(항목당 2~3장)**·숨쉬는 장·액션=빈 템플릿·COI 하단 한 줄.
  - `designer` = **★비주얼 게이트(매 장 X)**·메시지→그림(Zelazny)·추상은 은유(새로 추론)·그레이아웃·**틱덱 스타일 고정**·스쿼트.
- **references 갱신**: `visualization.md`(Zelazny+도형화·비주얼 게이트), `palettes.md`(라이트 펜톤·트렌드색), `writing-standard.md`(슬라이드 규칙·자연스러운 한국어·paid/earned 한글화).
- **자동 캡처 배선**: `render_deck.py`가 끝나며 `capture_deck.sh` 자동호출→PDF. 오케스트레이터 4층 QA = 그 PDF 필수+판정 기록 없으면 done X.
- **cold-start 테스트 결과(C 하네스):** ✅ 추론 전이됨(데이터→5트렌드 묶기·S/A 분리·한 메시지). ❌ **렌더러가 병목** — render_deck.py가 rich 비주얼(designer가 viz_intent로 dumbbell·before/after 골랐는데) 못 그림·검은띠·표지 placeholder·펜톤 아님. **= "논리=에이전트 ✓ / 코드=렌더러 ✗"**.
- **핵심 합의(후추님):** ① 시각화 비법은 *코드로 다 자동화 어려움* → 논리를 에이전트에 박고 *매번 추론*(코드=가드레일/렌더·기계). ② 학습한 *결과물(18장)은 템플릿으로 박지 말 것*(오버피팅) — 논리만. ③ 결과는 **틱덱 스타일**(미니맥스 옷 X).

## ⑥ git (다음 세션 sync 먼저)
- 브랜치 `deck/2026-mktg-v2-engine` · 미커밋 다수(agents 7개·contract_checks·SKILL·inbox 메모). 이전 6/28 빌드분 + 이번 편집분 섞임. **다음 세션: 정리 후 커밋 판단**(④ 결정 전엔 보류 가능).

## ⑦ 다음 세션 첫 행동(권장)
1. 이 파일 + `PRD_v4.md` 읽기.
2. **④ 미해결 3개를 후추님과 먼저 정리**(트랙 B냐 C냐·메모리 충돌·v3/v4). — 여기부터. 빌드 X.
3. 정해지면: C라면 render_deck.py 빌드(검은띠·표지·viz)·B라면 engine.py 경로. 어느 쪽이든 deck_v3에 다듬은 것(표지·훅·펩핀치·펜톤·paid-earned·출처1장)을 그 트랙에 반영.
