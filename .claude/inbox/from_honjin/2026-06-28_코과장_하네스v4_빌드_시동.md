# 코과장 시동 — TickDeck 하네스 v4 빌드 (2026-06-28)

## 1. 작업
`TickDeck/PRD_v4.md`를 **에이전트+스킬 하네스 파일들로 구현**한다. PRD = 설계 SoT. 먼저 PRD를 *전부* 읽고 그대로 따른다. 추측 금지.

## 2. 반드시 먼저 읽을 것
- `TickDeck/PRD_v4.md` (설계 정본 — §4 파이프라인·§5 에이전트·§6 장르·§7 계약·§11 안티패턴)
- `TickDeck/tickdeck_harness/knowledge/content/00_SYNTHESIS_콘텐츠방법론.md` (PART B 분석 렌즈 10종 — genre-trend-report 스킬의 분석 레시피 원천)
- `TickDeck/tickdeck_harness/knowledge/design/` (디자이너 참조용 디자인 캐논)

## 3. 만들 것 (manifest)
**에이전트 8** `TickDeck/.claude/agents/{name}.md` — frontmatter(name·description·tools·model) + 본문(핵심 역할·작업 원칙·입출력 프로토콜·에러 핸들링·팀 통신). 모델은 PRD §5 표 그대로(opus / 가벼운+코드).
`intake-director · collector · verifier · analyst · editorial-director · page-planner · designer · qa-reviewer`

**스킬** `TickDeck/.claude/skills/`
- `deck-harness/SKILL.md` — **오케스트레이터**(메인 트리거 스킬). ⓪~⑥ 워크플로 + 루프 A(재수집)·루프 B(페이지↔디자인) + 에이전트 팀 구성 + `_workspace/` 파일 전달 + 에러 핸들링 + 테스트 시나리오. description은 적극적·후속 키워드("다시/업데이트/보완") 포함.
- `genre-trend-report/SKILL.md` — ⓐ증거 프로필(Tier-A 출처·시계열/방향 데이터 타게팅·반대시각) + ⓑ분석 레시피(렌즈 10종·트렌드=상태전이) + 명제 틀(Tension→Diagnosis→Mechanism→Scenarios→Convergence→Action). 렌즈 내용은 위 방법론 PART B에서 가져와 references/로.
- `genre-topic-deck/SKILL.md` — 일반 주제 발표 프로필(주제 구조화·핵심 논점·청중 맞춤).
- `harness-contracts/SKILL.md` (+ `scripts/`) — Insight 스키마·명제 DAG 스키마 정의 + **5대 계약을 실제로 도는 test 코드로**(scripts/): C1 DAG 고아노드 검사·C3 상태전이 필드 검사·C4 Citation Tracker(evidence_ids≥2·source_overlap)·C2 메타표현 스캔·C5 순서 검증.

**CLAUDE.md** `TickDeck/CLAUDE.md`에 하네스 포인터(트리거 규칙 + 변경 이력)만 추가. 기존 내용 보존.

## 4. 컨벤션·규칙
- `.claude/commands/`는 만들지 말 것.
- 5대 계약(PRD §7)은 *프로즈*가 아니라 **돌아가는 test**로 구현(허술하면 의미 없음).
- 안티패턴(PRD §11) 절대 금지: 디자인-우선·dict 매칭 큐레이션·검증메타 콘텐츠화·정적통계를 트렌드로·단일출처 재포장·일회용 드라이버·텍스트 fallback·단방향.
- 중국 모델 호출은 신야 :online 격리 경유만(보안). collector 스킬에 명시.

## 5. 절대 금지
- PRD 안 읽고 추측해서 짜기. 막히면 멈추고 "여기 PRD에 안 정해짐" 보고.
- 같은 에러 2회 → 멈추고 보고.

## 6. 산출·검증·핸드오프
- 파일 생성 후 `harness-contracts/scripts/`의 계약 test를 *실제로 실행*해 통과 확인(미통과면 그 사실 명시).
- 결과를 `TickDeck/.claude/inbox/from_kogwajang/2026-06-28_하네스v4_빌드_결과.md`에: 만든 파일 목록 / 계약 test 실행 결과(통과·실패 수치) / 미정·막힌 부분 / 클차장 리뷰 요청.
