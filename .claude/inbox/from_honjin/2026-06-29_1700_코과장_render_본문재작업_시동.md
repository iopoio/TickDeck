# 코과장 시동 — render_deck 본문 재작업 (리뷰 34항목 중 *코드* 부분)

> 본부 설계 → 코과장 코드. cwd=/Users/hwa/Projects/Automation/TickDeck. **새 파일 0** — `render_deck.py`·`contract_checks.py`만 수정.
> 6갈래 리뷰 결과 = `/private/tmp/claude-501/-Users-hwa-Projects-Automation-Think/02dacc77-b142-4863-a468-54e539c86392/tasks/wuvbgztyy.output` (synthesis = 34 실행항목). 읽고 **코드로 고칠 것만** 처리한다(카피·차트 선택은 designer가 별도 단계).

## 코드로 고칠 것
1. **★원문자 필터 (후추님 반복 지적 · 재발 차단)**: `①-⑳`·`➀-➓`·`❶-❿`·`㉑-㉟` 등 enclosed numerals를
   - (a) `contract_checks.py`에 **C 계약**으로 검출 — rendered HTML과 deck_spec의 title/short_title/label/text에 원문자가 있으면 FAIL.
   - (b) `render_deck.py`가 혹시 들어와도 안전하게 일반 형식으로 치환(`①`→`1.`, 또는 제거).
   - 참조: `tickdeck_harness/knowledge/design/01_design_rules.md`·`tickdeck_harness/engine.py`의 기존 원문자 필터(B에는 있는데 v4로 안 옮겨짐).
2. **divider(간지) 개선** (리뷰 #21 — 현재 제목만·하단 데드스페이스): 막 번호/인덱스(예 PART 1·2·3)·3막 중 현재 위치 진행 인디케이터·제목 수직 중앙·옅은 배경 모티프. *제목 슬라이드가 아니라 '간지'답게.*
3. **index(목차) 개선** (리뷰 #22): 3 row(`01 설정` / `02 증거` / `03 행동`)·각 우측 한 줄 설명·콘텐츠 수직 중앙·항목 종결 통일·번호가 3막과 1:1.
4. **정렬 fix** (#18·#24): 점-화살표(shift) 차트의 퍼센트-노드라벨 겹침(최소 8px 여백)·캡션 위치(노드 라벨과 12px 수직 분리·하단 좌측 정렬).
5. (시간 남으면) **`_svg_funnel` 추가**: designer가 'A→B 깔때기'(예 광의 사용 88% → 생산 배포 72%)를 쓸 수 있게. 없으면 designer가 before_after로 대체하니 필수는 아님.

## 제약
- 새 파일 0. C6 유지. 무한루프: 같은 에러 2회 또는 같은 파일 3회+ 수정 시 멈추고 보고.
- 검증: `test_contracts.py` 통과 + 원문자 든 테스트 deck_spec으로 C 계약 FAIL 확인 + `theme:tech`로 렌더해 grid·개선된 divider·index 시각 구조 확인(grep).

## 보고
`.claude/inbox/from_kogwajang/2026-06-29_render_본문재작업_결과.md` — 항목별 무엇을 어떻게·검증 수치·미완 [표기].
