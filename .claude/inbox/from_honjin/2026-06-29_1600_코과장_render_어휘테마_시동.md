# 코과장 시동 — render_deck 레이아웃 어휘 + 주제 테마 강화 (복붙 탈출 구현)

> 본부(디자인 설계 전담) → 코과장(코드 타이핑). cwd=/Users/hwa/Projects/Automation/TickDeck. **새 파일 0** — `render_deck.py`만 수정.
> 배경: render_deck이 레이아웃 ~6개·테마 1개(펜톤)뿐이라 designer가 다양 양식을 명세해도 못 그려 모든 덱이 복붙됨(테크 덱이 마케팅 덱과 픽셀 동일). designer 사고(`references/visualization.md`)는 이미 강화됨(어휘 12·주제→테마). **이제 렌더러가 그걸 그릴 수 있게.** 디자인 결정은 본부가 아래에 다 못박았다 — 너는 그대로 코드화.

## 설계 (본부 결정 · 그대로 구현)

### 1. 주제 테마 시스템 (★복붙 탈출 핵심)
deck_spec 최상위에 `"theme"` 키(`tech`/`marketing`/`health`/`editorial` · 기본 `editorial`). render_deck의 PALETTES + `_css()`가 theme별로 분기:
- **tech**: 배경 `#FFFFFF` + 미세 그리드 라인(`linear-gradient` 48px·`rgba(31,78,121,.09)`) · 딥블루 `#1F4E79` · 시안 `#17A6C4` · **모서리 0(뾰족)** · eyebrow/footer는 모노폰트 · 좌상단 회로 모서리 마크(border-top+left). **참조 = 본부가 직접 짠 표지** `/private/tmp/claude-501/-Users-hwa-Projects-Automation-Think/02dacc77-b142-4863-a468-54e539c86392/scratchpad/tech_cover.html` (이 양식을 theme=tech의 표준으로).
- **marketing**: 따뜻한 화이트 + 코랄 · 모서리 크게 둥글게 · radial 블롭 배경(현 breeze 계열 + T1~T5 밴드는 marketing 전용으로 유지).
- **health**: 쿨 화이트 + 신뢰 블루 · 곡선 · 중간 모서리.
- **editorial**: 현 펜톤(기본·변경 없음).

### 2. 레이아웃 어휘 확장
현 `cover·statement·stat_grid·cards·hero_metric` + viz에 신규 `layout` enum 추가(`references/visualization.md` A 표 + GLM/MiniMax r2 CSS 참조):
- `split`(좌우 분할·대조) / `stepper`(순차 단계) / `node`(관계·다이어그램) / `matrix`(다기준 비교표) / `index`(목차) / `divider`(챕터 간지)
각 `_render_<name>` 함수 + CSS. content 블록(headline·body·bullets·metric·viz)은 최대 재사용.

### 3. cover를 theme별로
현 cover의 T1~T5 밴드 하드코딩 제거 → theme 분기. tech=회로 모서리+모노 eyebrow(밴드 X·표지에 그래프 장식 금지), marketing=T1~T5 밴드, 기타 theme별.

## 제약
- **C6 유지**: 수치·출처는 metric_id/src_id로만 · title/label/note raw 숫자 0.
- 새 파일 0(render_deck.py만). 무한루프: 같은 에러 2회 또는 같은 파일 3회+ 수정 시 멈추고 보고.
- **점진 OK**: theme 시스템 + tech 테마 + cover theme분기 + split·stepper·node·matrix 우선. 막히면 그 지점에서 보고.
- 검증: `test_contracts.py` 통과 + `theme:tech`로 표지 렌더해 grid 배경·딥블루·모노·밴드 없음 확인.

## 참조 (다 읽고 시작)
- `/Users/hwa/Projects/Automation/TickDeck/.claude/skills/deck-harness/references/visualization.md` — 어휘 12·동사신호·핵심질문→시각어휘·주제→테마·표지공식
- `/private/tmp/claude-501/-Users-hwa-Projects-Automation-Think/02dacc77-b142-4863-a468-54e539c86392/scratchpad/tech_cover.html` — 본부 직접 디자인한 테크 표지(theme=tech 표준)
- `/private/tmp/claude-501/-Users-hwa-Projects-Automation-Think/02dacc77-b142-4863-a468-54e539c86392/scratchpad/round2_answer.txt`(GLM 레이아웃 CSS) · `minimax_r2.txt`(MiniMax 어휘집 CSS)

## 보고
`.claude/inbox/from_kogwajang/2026-06-29_render_어휘테마_결과.md` — theme·레이아웃별 무엇을 어떻게·검증 수치·미완 [표기].
