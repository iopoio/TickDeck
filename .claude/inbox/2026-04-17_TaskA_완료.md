## Task A 완료 보고

### 변경 파일
- `backend/routers/auth.py`: `google_callback` 리다이렉트 방식으로 변경 (JWT 포함)
- `frontend/src/App.tsx`: `DEV_TOKEN` 제거, `react-router-dom` 라우팅 도입 (`/login`, `/auth/callback` 추가), JWT 연동
- `frontend/src/main.tsx`: `BrowserRouter` 추가

### 신규 파일
- `frontend/src/pages/LoginPage.tsx`: Google 로그인 버튼이 포함된 랜딩 페이지
- `frontend/src/pages/CallbackPage.tsx`: OAuth 콜백 처리 및 JWT 저장 로직
- `frontend/src/lib/auth.ts`: `localStorage` 기반 토큰 관리 유틸

### 검증 결과
- 빌드: `npm run build` 통과 ✅
- 수동 테스트: (로컬 서버 실행 후 URL 리다이렉트 흐름 확인 예정 - Task B 연동 후 수행)

### 커밋
- `feat: Google OAuth 프론트엔드 완성 + DEV_TOKEN 제거 (Task A)`
