# 2026-06-28 C render 날조 차단 결과

## 작업 요약
- render 층 content authority를 verifier/page-planner/story 쪽 ID 체계로 고정.
- designer를 layout/summary-only로 강등하고 HTML 직접 생성 권한 제거.
- `deck_spec.json + 02_verified registry -> render_deck.py -> C6 gate` 흐름 추가.

## 수정/생성 파일
- 수정: `.claude/agents/designer.md` — layout/프레젠테이션 only, 사실·수치·출처 창작/변경 금지, `deck_spec.json`만 산출.
- 수정: `.claude/agents/page-planner.md` — `short_title`, `allowed_source_ids`, `allowed_metric_ids` 출력 계약 추가.
- 수정: `.claude/agents/verifier.md` — `source_registry`, `metric_registry(metric_id -> value/unit/source_ids/scope)` 스키마 추가.
- 수정: `.claude/agents/analyst.md` — 입력명을 `02_verified.json`으로 갱신.
- 수정: `.claude/agents/qa-reviewer.md` — C1~C6 검수와 render authority 실패 조건 추가.
- 수정: `.claude/skills/deck-harness/SKILL.md` — designer→deck_spec→code render→C6→QA 파이프라인으로 갱신.
- 생성: `.claude/skills/deck-harness/scripts/render_deck.py` — registry 기반 HTML 렌더러. `metric_id` 값과 `src_id` 인용을 코드가 주입.
- 수정: `.claude/skills/harness-contracts/SKILL.md` — C6 계약 문서화.
- 수정: `.claude/skills/harness-contracts/scripts/contract_checks.py` — `validate_c6_content_authority` 추가.
- 수정: `.claude/skills/harness-contracts/scripts/test_contracts.py` — C6 + 렌더러 테스트 추가.

## C6 test 실행 결과
명령:

```bash
PYTHONDONTWRITEBYTECODE=1 python .claude/skills/harness-contracts/scripts/test_contracts.py
```

결과:
- Ran 15 tests
- PASS 15
- FAIL 0
- SKIP 0

추가 확인:
- `python -m compileall .claude/skills/harness-contracts/scripts .claude/skills/deck-harness/scripts` exit 0.
- 생성된 `__pycache__`는 제거했고, 최종 재테스트는 bytecode 생성 없이 실행.
- Gemini 교차 리뷰는 시도했으나 3분 이상 무출력이라 Ctrl-C로 중단(exit 130). 출력 없음, 외부 리뷰 [미확인].

## 막힌 점
- 작업 blocker 없음.
- 참고 run `_workspace/20260628_marketing2026/02_verified.json` 자체는 수정하지 않음. 이번 변경은 앞으로의 verifier 출력 스키마와 코드 계약을 고정한 것.
- 세션 시작용 fact-check 스크립트(`.claude/scripts/yangnyeon_factcheck.py`, `.claude/scripts/tracker_factcheck.py`)는 이 repo 안에 없어 실행 실패. 본 작업 검증과는 별개.
- Gemini 외부 리뷰는 CLI 무응답으로 미완료. 같은 무응답 반복을 피하려 재시도하지 않음.

## 클차장 확인 요청
- 기존 `_workspace/20260628_marketing2026/02_verified.json`를 새 `source_registry/metric_registry` 스키마로 백필할지 결정 필요.
- 기존 데모/과거 렌더 스크립트(`tickdeck_harness/marketing_full.py`, `demo_content.py` 등)는 수동 `출처:` 문자열을 아직 포함. 새 C6는 앞으로의 `deck_spec + render_deck.py` 산출물에 적용됨.
