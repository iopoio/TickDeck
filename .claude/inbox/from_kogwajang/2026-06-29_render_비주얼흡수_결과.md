# 2026-06-29 render 비주얼 흡수 결과

## 무엇을 왜 바꿨나
- `render_deck.py`: `viz` 블록 6종(`before_after`, `dumbbell`, `flow`, `big_number`, `gap_map`, `shift`)을 inline SVG로 렌더하도록 추가했다. 수치 텍스트는 `series[].metric_id`로 registry에서만 꺼내고, deck_v3의 모양과 배치만 범용화했다.
- `contract_checks.py`·`test_contracts.py`: `viz`를 C6 계약에 추가하고, chart enum·series metric_id·page allowlist·registry 존재·title/label/note raw number 금지를 테스트로 묶었다.
- `designer.md`·`deck-harness/SKILL.md`: designer가 수치·출처를 직접 쓰지 않고 `viz` chart enum과 `metric_id`만 내보내도록 출력 계약을 문서화했다.
- 같이 잡은 fix: 기본 테마를 라이트 펜톤 팔레트로 바꾸고, `body background:#111` 제거, `layout=cover`에서 `표지` role/eyebrow 노출 대신 제목·부제·T1~T5 밴드 lockup을 렌더하도록 분리했다.

## 검증 수치
- 계약 테스트: `python .claude/skills/harness-contracts/scripts/test_contracts.py` → 23/23 pass, fail 0, error 0.
- viz 렌더: `/private/tmp/tickdeck_viz_spec.json`(기존 `06_deck_spec_v2`의 viz_intent 2개를 `viz`로 변환) + `02_verified.json`으로 `render_deck.py` 실행 → exit 0, HTML 3 pages 생성.
- SVG 확인: `grep -c '<svg' /private/tmp/tickdeck_viz_output.html` → 2.
- C6 음성 테스트: registry 밖 `m_missing` → violation 1, allowlist 밖 `m_031` → violation 1.
- cover/background 확인: `grep -c '표지'` → 0, `grep -c 'background: #111'` → 0.
- py_compile: `render_deck.py`, `contract_checks.py`, `test_contracts.py` 3/3 exit 0. 이후 대상 스크립트 디렉토리 `__pycache__` count 0.

## 미검증
- [미검증] `render_deck.py`의 후속 PDF capture는 Chrome headless가 `Abort trap: 6`으로 실패했다. HTML 생성과 메모 8절의 SVG/grep/C6/py_compile 검증은 통과했지만, PDF 눈검수는 별도 Chrome 이슈 해결 후 필요하다.
