# TickDeck 엔진 R2 결과

## 변경 요약

- 계약: `source_registry[].short_name`, `metric_registry[].period`, `page_chrome: title_band`, viz `source_caption`/`title_style` 검증 추가.
- 렌더러: `source_caption:"on"`일 때 viz series metric 출처 합집합으로 `— {period} · per {short_name}` 자동 캡션 생성. 하단 source chip은 caption 커버 출처를 뒤로 정렬.
- 렌더러: `page_chrome:"title_band"` 상단 고정 밴드, divider 빈 밴드, cover/closing 제외.
- 렌더러: viz `title_style:"band"` chart-local 제목 밴드 추가.
- 게이트: `capture_deck.sh`에 `FIT_BAND_OVERFLOW`/`bandovf` 추가, `qa_log.py` 우선순위 반영.

## 테스트

- 시작 RED 재현: `python3 .claude/skills/harness-contracts/scripts/test_contracts.py` → 93 tests / 8 failures.
- targeted 계약 테스트: 3 tests / 0 failures.
- targeted 렌더 테스트: 4 tests / 0 failures.
- targeted capture gate 테스트: 1 test / 0 failures.
- 최종 fresh 전체 테스트: `python3 .claude/skills/harness-contracts/scripts/test_contracts.py` → 93 tests / 0 failures / 0 skipped.
- `git diff --check` → 0 errors.

## 데모 산출물

위치: `_workspace/20260707_engine_r2_demo/`

- `title_band_spec.json` / `title_band.html` / `title_band.pdf`
- `source_caption_spec.json` / `source_caption.html` / `source_caption.pdf`
- `title_style_band_spec.json` / `title_style_band.html` / `title_style_band.pdf`
- `registry.json`

검증:
- demo C6: 3 specs / 0 violations.
- HTML marker 확인:
  - `title_band.html`: body page + divider page에 `page-title-band`, `title-band`, 빈 divider `title-band-text`.
  - `source_caption.html`: `visual-source-caption` 및 `— 1Q26 · per IAB, Pew`.
  - `title_style_band.html`: `visual-title-band`, page chrome marker 없음.
- PDF 파일 존재 및 판독: 3 PDFs / 각 1 page.
- [미검증] Chrome 기반 HTML→PDF 캡처: Google Chrome 149 headless가 `Abort trap: 6`; Playwright Chromium은 `bootstrap_check_in ... Permission denied`; QuickLook은 sandbox initialization 실패. PDF는 ImageMagick fallback으로 생성.

## clo_v51 회귀

- 무옵션 재렌더: `_workspace/20260706_clo_v51/deck_r2_regression.html`
- page count: 기존 `deck_r1.html` 19 / 재렌더 19.
- body normalized SHA: 기존/재렌더 동일 `5d27fdcdbf7c43f247ae2e0364b50779a9b7292dd28d05049df111af2aa34745`.
- R2 marker in body: `page-title-band`, `title-band`, `visual-source-caption`, `visual-title-band` 모두 0.
- C6: 0 violations.
- [미검증] 픽셀/PDF visual diff: Chrome/Playwright/QuickLook 캡처가 현재 sandbox 권한에서 실패.

## 미검증/주의

- [미검증] 세션 시작 factcheck: `.claude/scripts/yangnyeon_factcheck.py`, `.claude/scripts/tracker_factcheck.py`가 이 repo에 없어 실행 실패.
- [미검증] Gemini 외부 코드 리뷰: CLI가 2분 이상 무출력으로 멈춰 `Ctrl-C` 중단.
- git commit은 하지 않음.
