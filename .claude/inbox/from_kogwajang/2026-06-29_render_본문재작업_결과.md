# 코과장 보고 — render_deck 본문 재작업 결과

## 요약

- 코드 변경: `.claude/skills/harness-contracts/scripts/contract_checks.py`, `.claude/skills/deck-harness/scripts/render_deck.py`
- 새 코드 파일: 0개
- 보고 파일: 본 파일 1개(후추님 지정 산출물)
- 추가 코드 수정 상태: `render_deck.py` 3회 패치 도달로 추가 코드 수정 중단. 현재 검증 기준 통과 항목만 보고.

## 처리 항목

1. 원문자 필터
   - C6에 enclosed numeral 패턴을 추가해 `①-⑳`, `➀-➓`, `❶-❿`, `㉑-㊿` 계열을 FAIL 처리.
   - `deck_spec` 문자열 전체를 훑어 `short_title`, `title`, `label`, `text` 포함 경로에서 원문자를 잡음.
   - rendered HTML 텍스트 노드도 C6에서 원문자를 잡음.
   - `render_deck.py`는 `_escape()` 단계에서 원문자를 `1.`, `2.` 형식으로 치환해 렌더 결과에 남지 않게 처리.

2. divider 간지 개선
   - divider를 어두운 간지 화면으로 분리.
   - `PART n / 3`, 3막 progress indicator, 우측 원형 motif 추가.
   - 제목 블록을 세로 중앙에 놓고 하단 데드스페이스를 줄임.

3. index 3막 1:1
   - list 항목을 `설정 / 증거 / 행동` 3행으로 파싱.
   - 각 행을 `번호 + 막 이름 + 우측 설명` 구조로 렌더.
   - 콘텐츠 블록을 수직 중앙 배치.

4. shift 차트 정렬
   - 라벨과 값 baseline을 분리해 최소 8px 이상 여백 확보.
   - `회색=기준 / 파랑=현재` 범례 추가.
   - note/caption은 SVG 하단 좌측에 두고 node label과 수직 분리.

5. `_svg_funnel` 추가
   - `funnel` chart type을 C6 supported chart와 renderer mapping에 추가.
   - metric registry 값만 사용하며 C6 0건으로 검증.

## 검증 결과

- RED 확인: 원문자 포함 deck_spec/rendered HTML이 수정 전 C6 위반 0건으로 통과함.
- `python -m py_compile ...`: exit 0.
- `python .claude/skills/harness-contracts/scripts/test_contracts.py`: 23 tests, 0 failures, 0 errors.
- 원문자 C6 테스트 deck_spec: 5 violations 확인(`short_title`, `text` 2곳, `label`, `rendered_html`).
- render_deck 치환 테스트: `has_enclosed False`, `has_replacement True`.
- 실제 tech 렌더: `/private/tmp/tickdeck_tech_verify.html`
  - grep 확인: `--grid-line`, `deck-theme-tech`, `index-row/index-part/index-copy`, `divider-progress`, `PART 2 / 3`, `PART 3 / 3`, `회색=기준`, `visual-shift`.
  - rendered HTML 원문자: False.
  - C6 결과: 2 violations. 둘 다 기존 deck_spec `p12/p13 short_title`의 `①`, `②` 원문자. 카피/덱 스펙 수정은 designer 영역이라 미수정.
- optional funnel smoke: `has_funnel True`, C6 violations 0.
- `git diff --check`: exit 0.

## 미완·미검증

- [미검증] Gemini 외부 리뷰: 90초 이상 무출력으로 중단, exit 130.
- [미확인] 세션 시작 factcheck 스크립트 2개: repo에 `.claude/scripts/yangnyeon_factcheck.py`, `.claude/scripts/tracker_factcheck.py`가 없어 둘 다 Errno 2.
- [미수정] 실제 deck_spec의 `p12/p13 short_title` 원문자 2건은 이번 C6가 의도대로 잡는 상태. 후속 designer/copy 단계에서 제거 필요.
