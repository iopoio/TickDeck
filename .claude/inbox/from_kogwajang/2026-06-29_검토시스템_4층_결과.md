# 2026-06-29 검토시스템 4층 결과

## 작업 요약
- TickDeck 하네스 1층 자동 체크에 `naturalness_check.py`를 추가.
- deck-harness 오케스트레이터에 4층 검토 구조를 문서화.
- writing-standard의 "자연스러운 한국어" 기준과 checker 기준을 맞춤.

## 생성/수정 파일
- 생성: `.claude/skills/harness-contracts/scripts/naturalness_check.py`
  - 마크다운 파일을 읽어 AI 번역투·빌려온 클리셰를 라인 단위로 출력.
  - `REGEX_BLACKLIST`, `COUNT_BLACKLIST` 상단 상수로 규칙 추가 가능.
- 생성: `.claude/skills/harness-contracts/scripts/test_naturalness.py`
  - 통과 케이스 1개, 탐지 케이스 1개.
- 수정: `.claude/skills/harness-contracts/SKILL.md`
  - naturalness checker 파일과 실행 명령 추가.
- 수정: `.claude/skills/deck-harness/SKILL.md`
  - `## 검토 단계 (4층)` 섹션 추가.
- 수정: `.claude/skills/deck-harness/references/writing-standard.md`
  - 자연스러운 한국어 섹션에 checker 동기화 기준과 `"~를 통해"` 파일당 8회 초과 기준 추가.
- 생성: `.claude/inbox/from_kogwajang/2026-06-29_검토시스템_4층_결과.md`

## naturalness 실행 결과
명령:

```bash
python .claude/skills/harness-contracts/scripts/naturalness_check.py _workspace/20260628_marketing2026_v2/REPORT_2026_marketing_trends_clear.md
```

결과:
- exit 0
- 출력 없음
- 걸린 항목 없음

## test 통과
명령:

```bash
PYTHONDONTWRITEBYTECODE=1 python .claude/skills/harness-contracts/scripts/test_naturalness.py
```

결과:
- Ran 2 tests
- PASS 2
- FAIL 0
- SKIP 0

기존 contract 회귀:

```bash
PYTHONDONTWRITEBYTECODE=1 python .claude/skills/harness-contracts/scripts/test_contracts.py
```

결과:
- Ran 18 tests
- PASS 18
- FAIL 0
- SKIP 0

추가 확인:
- `python -m py_compile .claude/skills/harness-contracts/scripts/naturalness_check.py .claude/skills/harness-contracts/scripts/test_naturalness.py` exit 0.
- `py_compile` 후 생성된 `__pycache__`는 제거.

## 막힌 점
- 작업 blocker 없음.
- 세션 시작 fact-check 스크립트 2개는 repo 안에 없어 실행 불가:
  - `.claude/scripts/yangnyeon_factcheck.py`
  - `.claude/scripts/tracker_factcheck.py`
- git working tree에 이번 작업 전/외부 변경으로 보이는 미커밋 파일이 다수 있음. 본 작업은 위 생성/수정 파일만 직접 건드림.

## 클차장 확인요청
- `당신의`는 후추님이 파일당 횟수 기준을 따로 주지 않아 현재 "등장 시 점검"으로 잡음. "N회 초과" 기준이 필요하면 `COUNT_BLACKLIST`로 옮기면 됨.
- `naturalness_check.py`는 걸린 항목이 있으면 exit 1, 없으면 exit 0으로 구현함. 1층 자동 gate에서 fail 신호로 쓰는 전제 OK인지 확인 필요.
