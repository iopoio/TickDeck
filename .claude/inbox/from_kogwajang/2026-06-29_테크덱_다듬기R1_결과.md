# 테크덱 다듬기 R1 결과 — 2026-06-29 17:24 KST

## 요약

- 목표: 1라운드 비저자 리뷰 평균 68점 약점 8개 우선순위 순서로 수정.
- 새 metric 추가: 0개. 수치 표기는 기존 `02_verified.json`의 `metric_id`만 사용.
- 소스 산출물 새 파일: 0개. 단, 본 결과 보고서는 후추님 지정 경로라 신규 작성.
- PDF 재생성: [미완] headless Chromium 계열 PDF 생성 오류가 2회 발생해 재시도 중단.

## 항목별 처리

1. 렌더버그 p10 gap_map
   - `.claude/skills/deck-harness/scripts/render_deck.py`의 `_svg_gap_map`에서 highlight 행 판정을 명시 변수로 분리하고, highlight 행의 숫자 파싱 실패 시 accent bar가 빈 폭으로 보이지 않도록 full-width fallback을 추가.
   - `_workspace/20260629_tech2026/06_deck_spec.json` p10에서 `metric_020`을 highlight, `metric_021`을 baseline으로 교정.
   - 렌더 HTML 확인: `metric_020` 값이 `visual-value-accent`로 렌더됨.

2. p13 양자 시각화
   - 양자 dumbbell 폐기.
   - `metric_024` 투자 공약과 `metric_025` 실측 사례를 별도 metric 카드로 분리.
   - “서로 다른 단위라 한 축에 놓지 않는다” callout 추가.

3. p14·p15 So-What
   - p14 flow placeholder 제거.
   - 실측 강도 × 전이 단계 판단 기준을 도구 AI, 인프라, 에이전틱, 휴머노이드·양자 4개 행동으로 채움.
   - 투자자·CIO·전략팀 행동 1줄 추가.
   - p15 빈 괄호 템플릿 제거.
   - 사내 AI 도구, agent workflow, GPU·infra, AI 보안 4개 실제 예시로 교체.

4. p04 정의 병기
   - Gartner 엄격 기준은 메인 dumbbell highlight로 유지.
   - McKinsey 확장 기준은 “보조 캡션” metric 카드로 낮춰 노출.
   - p04 note에 공개요약 기준, 실제 운영 배포, 확장 기준 보조 병기를 명시.

5. 인과 교정
   - 표지 부제를 “도구 전이 완료가 인프라 투자를 밀어 올리고, 자율화 미완이 거품 리스크를 얹는다” 흐름으로 교정.
   - `03_insights.json`의 `insight_003` claim/counter/narrative/mechanism 수정.
   - `04_dag.json` thesis, skeleton, node_infra, storyline을 같은 인과로 맞춤.

6. 반대신호 callout
   - p03에 생산성 반대신호 callout 추가.
   - `02_verified.json`에 해당 반대수치의 verified metric이 없고 rejected item으로만 있어 숫자 인용은 보류.
   - “전이 신호가 착시일 가능성”을 본문에서 직접 받도록 수정.

7. insight_005 묶음 완화
   - `03_insights.json`에서 휴머노이드는 본론 유지, 양자는 watchlist로 낮추는 구조로 수정.
   - `source_overlap_score`는 실제 겹침이 없으므로 0.0 유지.
   - `04_dag.json` node_frontier와 p13 deck copy도 같은 톤으로 교정.

8. 출처 톤다운
   - `02_verified.json`에서 Gartner source 조건에 공개요약·예측값 방향 신호 라벨 강화.
   - `metric_005`, `metric_007` 검증 메모에 메인 기준/예측값 표시 지시 추가.
   - `metric_014`는 IEA 페이지 기준값이며 다른 표와 기준 차이가 있을 수 있다고 낮춤.
   - p08 note도 IEA 기준 차이 가능성을 직접 표기.

## 검증

- 재렌더 명령: `python3 .claude/skills/deck-harness/scripts/render_deck.py _workspace/20260629_tech2026/06_deck_spec.json _workspace/20260629_tech2026/02_verified.json -o _workspace/20260629_tech2026/deck.html --theme pantone`
- 렌더 결과: HTML 15 pages, SVG 8개, HTML bytes 28449.
- C6 실제 deck+HTML 검증: `C6_COUNT 0`.
- JSON 문법 검증: `JSON_OK`.
- 계약 테스트: `test_contracts.py` 23 passed / 0 failed / 0 skipped.
- PDF/시각 QA: [미완] 기존 `deck.pdf`는 8 pages짜리 과거 산출물이라 이번 검증에서 제외. Chrome 자동 캡처 `Abort trap: 6`, Playwright Chromium `MachPortRendezvous ... Permission denied`로 headless Chromium 계열 오류가 2회 발생해 재시도 중단.

## 미완·리스크

- [미완] 새 PDF 렌더와 PDF 직접 시각 QA. HTML 렌더와 C6는 통과했지만, PDF는 headless Chromium 권한 문제 해결 후 다시 생성해야 함.
- [미완] 전역 factcheck 스크립트는 이 repo 경로에 없어 실행 불가: `.claude/scripts/yangnyeon_factcheck.py`, `.claude/scripts/tracker_factcheck.py`.
